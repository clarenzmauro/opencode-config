---
description: Use ripgrep (rg) and fd for fast searching instead of grep and find.
globs: "*.*"
alwaysApply: true
---

Use modern, high-performance search tools:

## Content Search (search inside files)
Use **rg** instead of `grep`:
- `rg "pattern"` instead of `grep "pattern"`
- `rg "pattern" ./src` instead of `grep -r "pattern" ./src`
- `rg -i "pattern"` for case-insensitive
- `rg "pattern" --type js` for file type filter

Benefits: 10-50x faster, respects .gitignore, skips binaries, multithreaded

## File Search (find files by name)
Use **fd** instead of `find`:
- `fd "pattern"` instead of `find . -name "*pattern*"`
- `fd -e "js"` for specific extension
- `fd -t d` for directories only
- `fd -t f` for files only

Benefits: Much faster, respects .gitignore, simpler syntax, parallel search